# GitHub 仓库合并与上传学习记录

日期：2026-09-10

## 这次要解决什么问题

工作目录原本是：

```text
four_serve/
├── -ros-/             # 已经是一个 Git 仓库，里面原先有 .git/
└── catkin_ws/         # 新建的 ROS 工作区，尚未被 Git 管理
```

目标是把两个同级目录作为**一个总仓库**上传到新的 GitHub 仓库，同时保留旧 `-ros-` 仓库已有的提交历史和分支。

最终目录是：

```text
four_serve/            # Git 仓库根目录，.git/ 在这里
├── .gitignore
├── -ros-/             # 原来的项目文件
└── catkin_ws/         # 新 ROS 工作区
    └── src/           # 需要提交的 ROS 源代码
```

## 最重要的判断：Git 到底管理哪一层

Git 从当前目录向上查找 `.git` 目录。谁拥有 `.git/`，谁就是仓库根目录。

这次最初的 `.git/` 位于 `-ros-/.git/`，所以：

- 在 `-ros-` 内执行 `git add`，只能管理 `-ros-` 内的文件；
- 同级的 `catkin_ws/` 不属于这个仓库，不能被提交；
- 不能直接在外层初始化 Git 又保留 `-ros-/.git/`，否则会出现嵌套仓库。外层 Git 会把内层仓库当成一个特殊项目（常见表现类似 submodule），而不是普通文件夹。

因此，如果要做一个总仓库，正确方向是让 `four_serve/.git/` 成为唯一的 Git 元数据目录。

## 先检查，而不是直接修改

```bash
git rev-parse --show-toplevel
```

- `git`：版本控制命令。
- `rev-parse`：读取、解析 Git 的内部信息。
- `--show-toplevel`：输出当前仓库的根目录绝对路径。

如果命令报 `not a git repository`，表示当前目录和它的上级目录都没有 `.git/`。这次在 `four_serve` 得到该错误，说明 Git 根目录不在总目录；继续查看子目录后发现根目录是 `four_serve/-ros-`。

```bash
git -C ./-ros- status --short
```

- `-C ./-ros-`：让 Git 先进入 `./-ros-` 再执行命令，不需要手动 `cd`。
- `status`：检查工作区、暂存区和最新提交之间的差异。
- `--short`：用精简格式显示；没有输出代表没有未提交改动。

```bash
git -C ./-ros- remote -v
```

- `remote`：查看远程仓库配置。
- `-v`：同时显示 fetch（下载）和 push（上传）地址。

这一步确认了旧仓库原本指向 `fengjielab/-ros-`。远程地址会随着 `.git/` 一起移动，因此必须在后续改成新总仓库地址。

## 为什么没有重新 `git init`

可以在 `four_serve/` 重新执行 `git init`，但这样会创建全新的历史，旧仓库的提交和分支不会自动继承。

这次选择移动 Git 元数据，保留历史：

```bash
mv -- ./-ros-/.git ./.git
```

- `mv`：移动文件或目录。
- `--`：表示后续内容全都是文件路径；即使文件名以 `-` 开头也不会被当成命令选项。
- `./-ros-/.git`：旧仓库的 Git 元数据目录。
- `./.git`：总目录的新 Git 元数据目录。

执行后，提交对象、分支、标签、远程配置都还保留在 `.git/` 内，只是 Git 根目录从 `-ros-` 上移到了 `four_serve`。

> 重要：移动后，不要再在 `-ros-` 内寻找 `.git/`；它已不是独立仓库，而是总仓库的普通子目录。

## 移动 `.git/` 后 Git 为什么显示大量删除

旧提交记录中的文件路径原来是：

```text
README.md
motor4_speed_monitor/
docs/
```

移动 Git 根目录后，实际文件路径变为：

```text
-ros-/README.md
-ros-/motor4_speed_monitor/
-ros-/docs/
```

因此 Git 初看会认为旧路径“删除了”，新路径“新增了”。当执行 `git add --all` 后，Git 会比较文件内容；内容一致时会在下一次提交中识别为重命名，例如：

```text
README.md => -ros-/README.md
```

这就是为什么旧提交历史仍然存在，而这次合并提交记录的是“文件位置发生变化”。

## ROS 项目必须忽略哪些生成文件

ROS/catkin 构建会产生大量机器生成文件，不应提交到 Git。总目录的 `.gitignore` 增加了：

```gitignore
# ROS/catkin generated build artifacts
catkin_ws/build/
catkin_ws/devel/
catkin_ws/install/
catkin_ws/log/
catkin_ws/.catkin_tools/

# Python cache files
__pycache__/
*.py[cod]
```

逐行说明：

- `# ...`：注释，Git 不处理。
- `catkin_ws/build/`：忽略 catkin 编译中间产物。
- `catkin_ws/devel/`：忽略开发环境生成文件。
- `catkin_ws/install/`：忽略安装空间生成文件。
- `catkin_ws/log/`：忽略构建日志。
- `catkin_ws/.catkin_tools/`：忽略 catkin-tools 自身状态目录。
- `__pycache__/`：忽略任意目录下 Python 字节码缓存目录。
- `*.py[cod]`：忽略 `.pyc`、`.pyo`、`.pyd` 等 Python 生成文件；`*` 表示任意文件名前缀，`[cod]` 表示三选一字符。

这不删除这些文件，只是使 Git 不把它们加入暂存区。源代码仍会保留，例如 `catkin_ws/src/fourdriver_base/`。

可用下面命令验证忽略规则：

```bash
git check-ignore -v catkin_ws/build catkin_ws/devel
```

- `check-ignore`：检查某路径是否命中 `.gitignore`。
- `-v`：显示是哪一个 `.gitignore` 的哪一行匹配了该路径。
- 后面的两个参数：要检查的目录路径。

## 暂存并检查这次改动

```bash
git add --all
```

- `add`：把工作区文件变更放入暂存区（staging area）。
- `--all`：处理全部新增、修改和删除；这里很重要，因为目录整理包含文件“从旧路径删除、在新路径新增”。

```bash
git diff --cached --stat
```

- `diff`：比较差异。
- `--cached`：比较“暂存区”和“最新提交”，即将要提交的内容。
- `--stat`：只给统计概要，适合先检查文件数量和增删情况。

```bash
git status --short
```

- `status`：再次查看状态。
- `--short`：简写状态；常见 `R` 是 rename（重命名），`A` 是 added（新增），`M` 是 modified（修改）。

这次检查结果是：原 `-ros-` 的文件被识别为移动到 `-ros-/`，`catkin_ws/src/` 的 ROS 源码被新增，`build/` 和 `devel/` 被忽略。

## 创建本地提交

```bash
git commit -m "Consolidate ROS workspace into repository"
```

- `commit`：把暂存区内容永久写入本地 Git 历史。
- `-m`：后面紧跟提交说明（message）。
- `"Consolidate ROS workspace into repository"`：本次提交的英文说明，含义是“将 ROS 工作区整合进仓库”。

本次生成的提交是：

```text
62c0067 Consolidate ROS workspace into repository
```

提交只写入本机 `.git/`，这一步还没有上传 GitHub。

## 更改远程仓库地址

旧 Git 元数据携带的是旧仓库地址 `fengjielab/-ros-`。总仓库需要上传到新地址 `fengjielab/-.git`，因此执行：

```bash
git remote set-url origin https://github.com/fengjielab/-.git
```

- `remote`：管理远程仓库。
- `set-url`：修改某个远程仓库的地址。
- `origin`：远程仓库的本地别名；Git 默认常用这个名字，但它不是 GitHub 用户名。
- `https://github.com/fengjielab/-.git`：新的 GitHub 仓库 HTTPS 地址。

检查是否改对：

```bash
git remote -v
```

应同时看到 fetch 和 push 都指向新仓库。

## 推送 `main` 分支

```bash
git push -u origin main
```

- `push`：上传本地提交到远程仓库。
- `-u`：`--set-upstream` 的缩写。建立本地 `main` 和远程 `origin/main` 的跟踪关系；以后在这个分支通常只需执行 `git push`。
- `origin`：刚才配置的新 GitHub 远程别名。
- `main`：本地要上传的分支名。

HTTPS 推送时：

- `Username` 输入 GitHub 用户名 `fengjielab`；
- `Password` 位置必须输入 GitHub Personal Access Token（PAT），不是 GitHub 登录密码；
- PAT 至少需要选择该仓库，并授予 `Contents: Read and write`。

如果报 `403 Permission denied`，通常表示 Token 没有选中目标仓库、没有 `Contents` 写权限、Token 不是该账号创建，或远程地址仍指向错误仓库。

## 推送旧的其他分支

这次旧仓库中还有一个本地分支：

```text
无线esp32分支
```

它不会因为 `main` 被推送而自动上传。要单独推送：

```bash
git push -u origin 无线esp32分支
```

- 前三个参数的含义和推送 `main` 相同。
- `无线esp32分支`：要上传的本地分支名称。

推送分支**不等于合并分支**。这样做只是把该分支也备份到新 GitHub 仓库。若以后要把它纳入主线，应先检查差异和冲突，再在 `main` 上执行合并，或在 GitHub 创建 Pull Request。

## 数据流：文件如何到达 GitHub

```text
工作区文件
  four_serve/-ros- 和 four_serve/catkin_ws/src
          │ git add --all
          ▼
暂存区（index）
          │ git commit -m "..."
          ▼
本地仓库历史（four_serve/.git）
  main、无线esp32分支、所有旧提交
          │ git push -u origin <分支>
          ▼
GitHub 远程仓库（origin）
  https://github.com/fengjielab/-.git
```

`.gitignore` 位于“工作区到暂存区”的入口：它阻止 `build/`、`devel/` 等生成文件进入暂存区；它不会阻止已经提交过的文件，也不会删除电脑上的文件。

## 合并后仓库发生了什么改变

1. `.git/` 从 `-ros-/.git/` 移到 `four_serve/.git/`，因此总目录成为 Git 根目录。
2. 原项目文件路径都多了一层 `-ros-/`；Git 在提交 `62c0067` 中记录了这些重命名。
3. 新增 `catkin_ws/.catkin_workspace` 和 `catkin_ws/src/fourdriver_base/` 等 ROS 源文件。
4. 新增总目录 `.gitignore`，防止 ROS 构建产物和 Python 缓存被上传。
5. `origin` 从旧仓库 `fengjielab/-ros-` 改为新总仓库 `fengjielab/-.git`。
6. `main` 和 `无线esp32分支` 都建立了各自的远程跟踪分支。
7. 旧 GitHub 仓库 `fengjielab/-ros-` 没有被删除或修改，它仍可作为历史备份；确认新仓库正常后再决定保留、归档或删除。

## 下次遇到相同情况的决策方法

先问自己一句：**这些文件夹要不要共享同一套提交历史、分支和远程仓库？**

- 要共享：建立一个总目录仓库，并确保其内部没有第二个 `.git/`；若已有仓库并且要保留历史，移动或迁移它的 `.git/`，再提交目录重组。
- 不共享：每个项目分别 `git init`，分别设置自己的 GitHub 远程地址。
- 只想把一个独立仓库嵌入另一个，但仍独立维护：使用 Git submodule；这比普通目录合并更复杂，不适合本次需求。

每次推送前都执行：

```bash
git status
git remote -v
git branch -vv
```

- `git status`：确认有没有遗漏或误加入的文件。
- `git remote -v`：确认不会推到错误的 GitHub 仓库。
- `git branch -vv`：确认当前分支、最近提交，以及它跟踪的远程分支。

这三个检查能避免本次最容易出现的两个问题：推到旧仓库，以及忘记推送额外分支。
