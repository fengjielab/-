# Git 代理配置记录（2026-09-10）

## 一、背景

在克隆 GitHub 仓库时遇到连接失败：

```
fatal: unable to access 'https://github.com/fengjielab/-.git/':
Failed to connect to github.com port 443 via 127.0.0.1 after 2052 ms:
Could not connect to server
```

排查后发现：Git 全局配置了失效代理 `http://127.0.0.1:7892`，但该代理软件没有运行（端口 7892 未监听）。

而直连 GitHub 是正常的（`github.com:443` 连接成功，返回 `HTTP/1.1 200 OK`）。

## 二、问题排查过程

### 1. 查看当前代理配置

```powershell
git config --global --get http.proxy
git config --global --get https.proxy
```

### 2. 测试代理端口是否监听

```powershell
Test-NetConnection -ComputerName 127.0.0.1 -Port 7892
```

结果：`TcpTestSucceeded: False` → 代理未运行。

### 3. 测试直连 GitHub 是否正常

```powershell
Test-NetConnection -ComputerName github.com -Port 443
```

结果：`TcpTestSucceeded: True` → 直连正常。

## 三、去掉失效代理

```powershell
git config --global --unset http.proxy
git config --global --unset https.proxy
```

验证是否清除干净：

```powershell
git config --global --list | Select-String -Pattern 'proxy'
# 无输出即为清除成功
```

## 四、设置新代理（127.0.0.1:7897）

```powershell
git config --global http.proxy http://127.0.0.1:7897
git config --global https.proxy http://127.0.0.1:7897
```

验证：

```powershell
git config --global --get http.proxy   # http://127.0.0.1:7897
git config --global --get https.proxy  # http://127.0.0.1:7897
```

## 五、常用命令速查

| 操作 | 命令 |
| --- | --- |
| 设置全局代理 | `git config --global http.proxy http://127.0.0.1:7897` |
| 设置全局 HTTPS 代理 | `git config --global https.proxy http://127.0.0.1:7897` |
| 查看当前代理 | `git config --global --get http.proxy` |
| 去掉全局代理 | `git config --global --unset http.proxy` |
| 仅对当前仓库设置 | `git config http.proxy http://127.0.0.1:7897`（不加 `--global`） |
| 仅对当前仓库取消 | `git config --unset http.proxy` |

## 六、常见代理端口对照

| 端口 | 说明 |
| --- | --- |
| `7890` / `7897` | Clash / Clash Verge 默认端口 |
| `10809` | V2rayN 的 HTTP 代理端口 |
| `10808` | V2rayN 的 SOCKS5 代理端口（需写成 `socks5://127.0.0.1:10808`） |
| `1080` | 通用 SOCKS5 端口 |

## 七、注意事项

1. **设置代理后必须确保代理软件在运行**，否则 Git 访问会再次失败。
2. 直连 GitHub 正常时可临时绕过代理克隆：
   ```powershell
   git -c http.proxy= -c https.proxy= clone <仓库地址>
   ```
3. SOCKS5 代理写法与 HTTP 不同，需写协议头：`socks5://127.0.0.1:端口`。
