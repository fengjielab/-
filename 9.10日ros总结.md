# 9.10 日 ROS 底盘封装总结

## 1. 项目目标

当前项目的目标是把已经完成的 STM32 四电机底盘控制，逐步封装成 ROS 机器人底盘，使电脑可以通过键盘或其他 ROS 节点发布速度指令，由 Nano 接收并通过串口转发给 STM32，最终控制四个电机。

目标控制链路：

```text
电脑键盘
  ↓
ROS 键盘控制节点
  ↓  发布 /cmd_vel
Nano 上的 ROS 底盘节点
  ↓  串口 /dev/ttyUSB0
STM32 USART1
  ↓
PWM、编码器、PID、电机驱动
  ↓
四个麦克纳姆轮
```

当前采用的总体架构是：

```text
Nano 运行 ROS Melodic
STM32 继续负责底层实时电机控制
Nano 负责 ROS 接口和串口桥接
电脑负责键盘或上层控制
```

---

## 2. 当前硬件和软件环境

### Nano 环境

- Ubuntu：18.04.6 LTS
- 架构：`aarch64`
- ROS：Melodic
- ROS 构建系统：ROS 1 `catkin`
- 当前用户：`abot`

### STM32 工程

当前文件夹中的 STM32 工程为：

```text
motor4_speed_monitor/
```

主要特点：

- MCU：STM32F103ZET6
- 开发环境：STM32CubeMX / STM32CubeIDE
- 库：STM32 HAL
- 系统：FreeRTOS
- 串口：USART1
- 波特率：115200
- 数据格式：8 位数据、无校验、1 位停止位，即 8N1
- USART1 引脚：
  - PA9：USART1_TX
  - PA10：USART1_RX

### Nano 串口设备

USB 转串口插入 Nano 后识别为：

```text
/dev/ttyUSB0
```

设备权限显示为：

```text
root dialout
```

因此当前用户需要属于 `dialout` 用户组，才能直接访问该串口。

检查用户组：

```bash
groups
```

如果没有 `dialout`，可以执行：

```bash
sudo usermod -aG dialout $USER
```

然后退出 SSH 并重新登录。

---

## 3. TCP、UDP、UART/USART 的区别

### TCP

TCP 是 `Transmission Control Protocol`，即“传输控制协议”。

数据路线：

```text
应用程序
  ↓
TCP 连接
  ↓
IP 网络
  ↓
目标设备
```

特点：

- 需要建立连接
- 数据可靠到达
- 数据按顺序到达
- 丢包会重传
- 适合可靠控制、配置和状态传输

SSH 本身使用 TCP。

### UDP

UDP 是 `User Datagram Protocol`，即“用户数据报协议”。

数据路线：

```text
应用程序
  ↓
UDP 数据报
  ↓
IP 网络
  ↓
目标设备
```

特点：

- 不需要建立连接
- 不保证一定到达
- 不保证顺序
- 不会自动重传
- 开销小、延迟低
- 适合实时遥控、传感器数据和周期性状态信息

控制车辆时，UDP 通常需要配合心跳包和断线自动停车机制。

### UART/USART

UART/USART 是串口通信外设或串行通信方式，不属于 TCP 或 UDP。

数据路线：

```text
设备 A TX
  ↓ 电平信号
设备 B RX
```

常见接线：

```text
设备 A TX → 设备 B RX
设备 A RX ← 设备 B TX
设备 A GND ─ 设备 B GND
```

当前项目中实际使用的是：

```text
Nano USB 串口设备 ↔ STM32 USART1
```

如果 ESP32 使用 UART 连接 Nano 或 STM32，属于串口通信；如果 ESP32 使用 Wi-Fi，则 ESP32 也可以运行 TCP 或 UDP。两者可以组合：

```text
电脑
  ↓ Wi-Fi + TCP/UDP
ESP32
  ↓ UART
STM32
```

---

## 4. 不同键盘转接方式的数据路线

### 4.1 SSH 标准输入

```text
电脑键盘
  ↓
电脑操作系统
  ↓
SSH 客户端
  ↓ SSH 加密连接
Nano SSH 服务
  ↓
伪终端 stdin
  ↓
Nano 控制程序
```

这种方式传输的是终端字符，不是原始键盘设备事件。

适合简单的 `W/S/A/D` 控制。通常使用：

```bash
ssh -t 用户名@Nano地址
```

### 4.2 TCP

```text
电脑键盘
  ↓
电脑端键盘监听程序
  ↓
TCP 数据
  ↓
网络/Wi-Fi
  ↓
Nano TCP 服务端
  ↓
Nano 控制程序
```

### 4.3 UDP

```text
电脑键盘
  ↓
电脑端键盘监听程序
  ↓
UDP 数据报
  ↓
网络/Wi-Fi
  ↓
Nano UDP 接收程序
  ↓
Nano 控制程序
```

### 4.4 ROS

```text
电脑键盘
  ↓
键盘 ROS 节点
  ↓
/cmd_vel
  ↓ ROS 1 网络通信
Nano ROS 节点
  ↓
电机控制接口
```

ROS 1 节点之间底层通常使用 TCPROS 传输消息，因此电脑和 Nano 需要处于可互相访问的网络中。

### 4.5 USB/IP

```text
电脑键盘
  ↓
电脑 USB 和操作系统
  ↓
USB/IP 网络转发
  ↓
Nano 虚拟 USB 设备
  ↓
evdev
  ↓
Nano 控制程序
```

这种方式传输的是较底层的键盘设备事件，配置较复杂，当前项目不优先采用。

### 4.6 远程桌面

```text
电脑键盘
  ↓
远程桌面客户端
  ↓
网络
  ↓
Nano 远程桌面服务
  ↓
Nano 图形界面程序
```

---

## 5. 当前项目实际使用的底层控制链路

### STM32 内部控制链路

```text
USART1 接收字节
  ↓
HAL_UART_Receive_IT()
  ↓
HAL_UART_RxCpltCallback()
  ↓
uart_last_cmd
  ↓
StartDefaultTask()
  ↓
Robot_Forward / Robot_Backward / Robot_Left / Robot_Right
  ↓
Robot_SetVelocity()
  ↓
Motor_SetTarget()
  ↓
Motor_ControlStep()
  ↓
编码器反馈、PID、PWM
  ↓
电机驱动器和电机
```

### STM32 当前支持的单字节指令

```text
W / w：前进
S / s：后退
A / a：左移
D / d：右移
Q / q：原地左转
E / e：原地右转
X / x：停止
```

当前 STM32 的指令处理位于 FreeRTOS 任务中，收到字符后调用对应的机器人动作函数。

### 当前麦轮速度映射

`robot.c` 中已有基础运动学映射：

```text
m1 = vx + vy - wz
m2 = vx - vy + wz
m3 = vx - vy - wz
m4 = vx + vy + wz
```

其中：

- `vx`：车体前后方向速度
- `vy`：车体左右方向速度
- `wz`：车体绕垂直轴的角速度

---

## 6. 已经完成的实际测试

### 6.1 Nano 系统和 ROS 检查

已经确认：

```text
Ubuntu 18.04.6 LTS
aarch64
ROS Melodic
```

### 6.2 Nano 检测到 USB 串口

执行串口检查后发现：

```text
/dev/ttyUSB0
```

没有 `/dev/ttyACM*` 是正常的，因为当前 USB 转串口设备使用的是 `ttyUSB0`。

### 6.3 STM32 → Nano 接收测试

在 Nano 上配置串口：

```bash
stty -F /dev/ttyUSB0 115200 cs8 -cstopb -parenb -ixon -ixoff -crtscts
```

然后监听：

```bash
timeout 5 cat /dev/ttyUSB0
```

STM32 复位或重新上电后，Nano 成功收到：

```text
Motor speed monitor started!
```

完整路线：

```text
STM32 HAL_UART_Transmit()
  ↓
STM32 USART1_TX / PA9
  ↓
USB 转串口模块 RX
  ↓
USB
  ↓
Nano /dev/ttyUSB0
  ↓
cat
```

### 6.4 Nano → STM32 发送停止指令测试

执行：

```bash
printf 'X' > /dev/ttyUSB0
```

该指令会让 STM32 执行 `Robot_Stop()`。

完整路线：

```text
Nano printf
  ↓
/dev/ttyUSB0
  ↓ USB 转串口模块 TX
  ↓
STM32 USART1_RX / PA10
  ↓
HAL_UART_Receive_IT()
  ↓
HAL_UART_RxCpltCallback()
  ↓
uart_last_cmd = 'X'
  ↓
Robot_Stop()
```

### 6.5 Nano 直接发送运动指令测试

已经测试过：

```bash
printf 'W' > /dev/ttyUSB0
```

随后发送：

```bash
printf 'X' > /dev/ttyUSB0
```

这证明 Nano 通过串口可以控制 STM32 运动和停车。

注意：当前 STM32 收到 `W` 后会保持目标速度，单独发送一次 `W` 不会自动停车，因此测试时必须随后发送 `X`。测试时应让车轮离地并准备断电开关。

---

## 7. ROS 工作空间和功能包结构

### 工作空间和 STM32 工程的区别

STM32 工程由 STM32CubeIDE 编译，目标是 STM32 芯片；ROS 工作空间由 `catkin_make` 编译，目标是 Nano 上的 Linux/ROS。

二者可以放在同一个 Git 总仓库中，但不应把 ROS 文件放进 STM32 的 `Core`、`Drivers` 等目录。

推荐总结构：

```text
fourdriver/
├── motor4_speed_monitor/     # STM32 CubeIDE 工程
├── ros_ws/                   # ROS 工作空间
│   └── src/
│       └── fourdriver_base/  # ROS 底盘功能包
├── docs/
└── README.md
```

### ROS 工作空间目录

```text
catkin_ws/
├── src/       # 源代码和 ROS 功能包
├── build/     # catkin 编译中间文件，自动生成
└── devel/     # 开发环境文件，自动生成
```

`build/` 和 `devel/` 不是手写源码；它们由 `catkin_make` 自动生成。

每打开一个新的终端，通常需要加载：

```bash
source /opt/ros/melodic/setup.bash
source ~/Desktop/four_serve/catkin_ws/devel/setup.bash
```

当前实际使用的 Nano 工作空间路径为：

```text
~/Desktop/four_serve/catkin_ws
```

### ROS 功能包

当前功能包目录为：

```text
~/Desktop/four_serve/catkin_ws/src/fourdriver_base
```

`package.xml` 中的真实包名为：

```text
fourdriver_base
```

之前出现过 `fourdrive_base` 和 `fourdriver_base` 的拼写混用，导致 ROS 找到了旧工作空间中的功能包。后续必须统一使用：

```text
fourdriver_base
```

创建和编译功能包的基本流程：

```bash
cd ~/Desktop/four_serve/catkin_ws/src
catkin_create_pkg fourdriver_base rospy geometry_msgs

cd ~/Desktop/four_serve/catkin_ws
catkin_make
source devel/setup.bash
rospack find fourdriver_base
```

---

## 8. 当前 ROS 节点

### 最小测试节点

最开始创建了一个只打印日志的节点，用来确认 ROS 功能包和 Python 节点可以正常启动。

运行方式：

```bash
rosrun fourdriver_base fourdriver_base_node.py
```

成功输出过：

```text
fourdriver_base node started
```

### 串口桥接节点

当前创建的脚本为：

```text
src/fourdriver_base/scripts/fourdriver_serial_nano.py
```

它的功能是：

```text
/cmd_vel
  ↓
fourdriver_serial_nano.py
  ↓
/dev/ttyUSB0
  ↓
STM32
```

启动方式：

```bash
cd ~/Desktop/four_serve/catkin_ws
source /opt/ros/melodic/setup.bash
source devel/setup.bash
rosrun fourdriver_base fourdriver_serial_nano.py
```

正常启动信息：

```text
Serial connected: /dev/ttyUSB0 at 115200 baud
Subscribed to /cmd_vel
```

### 当前 `/cmd_vel` 到单字节指令的映射

串口桥接节点当前根据速度方向选择指令：

```text
linear.x > 0  → W
linear.x < 0  → S
linear.y > 0  → A
linear.y < 0  → D
angular.z > 0 → Q
angular.z < 0 → E
全部接近 0    → X
```

当前实现采用“主要方向优先”的简化判断，因此暂时不能同时表达前进、横移和旋转的组合速度。

例如，虽然发布：

```text
linear.x = 0.2
```

当前实际只会发送字符 `W`，不会把 `0.2` 这个速度数值发送给 STM32。

目前 STM32 中的速度仍然是固定值 `150`。后续需要设计连续速度串口协议，才能完整利用 ROS 的 `vx`、`vy`、`wz` 和麦轮运动学。

---

## 9. 已经完成的 ROS 测试

### 9.1 ROS 节点订阅测试

串口桥接节点已经成功启动，并成功订阅：

```text
/cmd_vel
```

### 9.2 ROS 停车指令测试

发布全零速度：

```bash
rostopic pub -1 /cmd_vel geometry_msgs/Twist "{linear: {x: 0.0, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.0}}"
```

ROS 节点收到消息后发送：

```text
X
```

完整路线：

```text
rostopic pub
  ↓
/cmd_vel
  ↓
fourdriver_serial_nano.py
  ↓
/dev/ttyUSB0
  ↓
STM32 USART1
  ↓
Robot_Stop()
```

### 9.3 ROS 前进测试

测试命令：

```bash
rostopic pub -1 /cmd_vel geometry_msgs/Twist "{linear: {x: 0.2, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.0}}"
```

当前节点会发送：

```text
W
```

随后必须发布全零速度，使节点发送：

```text
X
```

测试车辆时需要：

- 车轮离地
- 底盘固定
- 电机电源已接通
- 手边有断电开关
- 不要让车轮直接接触地面

---

## 10. 原有电脑键盘脚本的现状

当前仓库中的：

```text
keyboard_control.py
```

使用了：

- `pynput` 监听键盘
- `pyserial` 打开 `/dev/ttyUSB0`
- 每 50 ms 发送一次当前单字节指令

它当前的路线是：

```text
电脑键盘
  ↓
pynput
  ↓
电脑上的 /dev/ttyUSB0
  ↓
STM32
```

这个脚本适合“电脑直接通过 USB 串口连接 STM32”的测试方式；如果最终改成 ROS 架构，并且 USB 转串口插在 Nano 上，那么电脑端不应该继续直接打开 `/dev/ttyUSB0`。

后续有两种处理方式：

### 方式一：使用现成 ROS 键盘节点

```text
电脑键盘
  ↓
teleop_twist_keyboard
  ↓
/cmd_vel
  ↓ 网络
Nano ROS 串口桥接节点
  ↓
STM32
```

### 方式二：把原脚本改成 ROS 键盘发布节点

```text
电脑键盘
  ↓ pynput
电脑 ROS 键盘节点
  ↓ 发布 /cmd_vel
Nano ROS 串口桥接节点
  ↓
STM32
```

---

## 11. 当前还没有完成的部分

### ROS 工程方面

- 正式整理 ROS package 的 `package.xml`
- 完善 `CMakeLists.txt`
- 增加 `launch` 启动文件
- 增加串口参数配置文件
- 设置开机自动加载工作空间环境
- 整理 Nano 和电脑之间的 ROS 网络配置

### 通信协议方面

- 当前只有单字节方向指令
- 还没有连续目标速度协议
- 还没有帧头、长度、校验和或错误码
- 还没有串口回复确认机制
- 还没有串口断线处理
- 还没有 ROS 指令超时自动停车

### 底盘反馈方面

- STM32 还没有通过串口向 Nano 发布完整编码器数据
- 还没有 `/odom`
- 还没有 `tf`
- 还没有轮速反馈消息
- 还没有把四轮实际速度转换成 ROS 里程计

### 机器人模型和导航方面

- 还没有 URDF
- 还没有 RViz 底盘模型
- 还没有 IMU 接口
- 还没有 SLAM
- 还没有导航栈

---

## 12. 推荐的后续学习顺序

```text
1. 确认 ROS package 和 workspace 名称
2. 保证串口桥接节点可以稳定启动
3. 用 /cmd_vel 测试 W/S/A/D/Q/E/X
4. 增加 cmd_vel 超时自动发送 X
5. 接入电脑键盘 ROS 节点
6. 设计连续速度串口协议
7. 修改 STM32 解析连续速度指令
8. 通过串口发送编码器反馈
9. 在 Nano 上发布 /odom
10. 发布 odom → base_link 的 tf
11. 创建 URDF 和 RViz 模型
12. 再进入 SLAM 和导航
```

---

## 13. 当前最重要的认识

1. ROS 工作空间和 STM32 CubeIDE 工程是两类不同工程。
2. 两者可以放在同一个总仓库中，但应保持目录和构建系统独立。
3. TCP/UDP 是网络传输协议，UART/USART 是设备串口通信方式。
4. SSH 不会自动把电脑的原始键盘设备映射成 Nano 的 `/dev/input` 设备。
5. 当前最合适的架构是“电脑发布 ROS 指令，Nano 做 ROS 到串口的桥接，STM32 做实时电机控制”。
6. 当前 ROS 链路已经成功完成了 `/cmd_vel` 到 STM32 停车指令 `X` 的传输。
7. 当前前进测试已经可以通过 ROS 发送 `W`，但速度值仍然是 STM32 中固定的 `150`。
8. 真正完整的 ROS 麦轮底盘还需要连续速度协议、编码器反馈、里程计和安全停车机制。

---

## 14. 命令和数据结构格式

### 14.1 STM32 当前单字节串口指令

当前 STM32 接收的是一个字节，不需要加空格，也不需要加换行：

```text
W
```

发送示例：

```bash
printf 'W' > /dev/ttyUSB0
```

命令表：

| 字符 | 含义 | STM32 调用函数 |
|---|---|---|
| `W` 或 `w` | 前进 | `Robot_Forward(150)` |
| `S` 或 `s` | 后退 | `Robot_Backward(150)` |
| `A` 或 `a` | 左移 | `Robot_Left(150)` |
| `D` 或 `d` | 右移 | `Robot_Right(150)` |
| `Q` 或 `q` | 原地左转 | `Robot_TurnLeft(150)` |
| `E` 或 `e` | 原地右转 | `Robot_TurnRight(150)` |
| `X` 或 `x` | 停止 | `Robot_Stop()` |

当前指令没有参数。例如：

```text
W150
```

目前不会被解析成“以 150 速度前进”，STM32 只会先处理 `W`，后面的字符需要另行处理。因此当前版本只能控制固定速度 `150`。

### 14.2 当前 ROS `geometry_msgs/Twist` 数据结构

`/cmd_vel` 的消息类型为：

```text
geometry_msgs/Twist
```

内部结构为：

```text
Twist
├── linear
│   ├── x
│   ├── y
│   └── z
└── angular
    ├── x
    ├── y
    └── z
```

参数含义：

| 参数 | 含义 | 单位 | 当前底盘用途 |
|---|---|---|---|
| `linear.x` | 车体前后线速度 | m/s | 正数前进，负数后退 |
| `linear.y` | 车体左右线速度 | m/s | 正数左移，负数右移 |
| `linear.z` | 垂直方向线速度 | m/s | 当前不使用，填 0 |
| `angular.x` | 绕 X 轴角速度 | rad/s | 当前不使用，填 0 |
| `angular.y` | 绕 Y 轴角速度 | rad/s | 当前不使用，填 0 |
| `angular.z` | 绕 Z 轴角速度 | rad/s | 正数左转，负数右转 |

对于平面麦轮底盘，通常只填写：

```text
linear.x
linear.y
angular.z
```

其他三个参数填 0：

```text
linear.z   = 0
angular.x  = 0
angular.y  = 0
```

### 14.3 ROS 停止命令

完整命令：

```bash
rostopic pub -1 /cmd_vel geometry_msgs/Twist "{linear: {x: 0.0, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.0}}"
```

参数解释：

| 参数 | 含义 |
|---|---|
| `rostopic pub` | 使用命令行发布 ROS 消息 |
| `-1` | 只发布一次后退出 |
| `/cmd_vel` | 要发布的 ROS 话题名称 |
| `geometry_msgs/Twist` | 话题消息类型 |
| 后面的 `{...}` | Twist 消息的具体字段值 |

消息内容：

```yaml
linear:
  x: 0.0
  y: 0.0
  z: 0.0
angular:
  x: 0.0
  y: 0.0
  z: 0.0
```

当前节点会把这条消息转换为：

```text
X
```

### 14.4 ROS 前进命令

```bash
rostopic pub -1 /cmd_vel geometry_msgs/Twist "{linear: {x: 0.2, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.0}}"
```

关键参数：

```text
linear.x = 0.2
```

表示期望前进速度为 `0.2 m/s`。

但是当前桥接节点只判断正负方向，不会把 `0.2` 传给 STM32，因此实际转换为：

```text
W
```

STM32 最终使用固定目标速度：

```text
Robot_Forward(150)
```

### 14.5 ROS 后退命令

```bash
rostopic pub -1 /cmd_vel geometry_msgs/Twist "{linear: {x: -0.2, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.0}}"
```

转换关系：

```text
linear.x < 0 → S → Robot_Backward(150)
```

### 14.6 ROS 左移和右移命令

左移：

```bash
rostopic pub -1 /cmd_vel geometry_msgs/Twist "{linear: {x: 0.0, y: 0.2, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.0}}"
```

转换关系：

```text
linear.y > 0 → A → Robot_Left(150)
```

右移：

```bash
rostopic pub -1 /cmd_vel geometry_msgs/Twist "{linear: {x: 0.0, y: -0.2, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.0}}"
```

转换关系：

```text
linear.y < 0 → D → Robot_Right(150)
```

### 14.7 ROS 左转和右转命令

原地左转：

```bash
rostopic pub -1 /cmd_vel geometry_msgs/Twist "{linear: {x: 0.0, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.2}}"
```

转换关系：

```text
angular.z > 0 → Q → Robot_TurnLeft(150)
```

原地右转：

```bash
rostopic pub -1 /cmd_vel geometry_msgs/Twist "{linear: {x: 0.0, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: -0.2}}"
```

转换关系：

```text
angular.z < 0 → E → Robot_TurnRight(150)
```

### 14.8 持续发布命令

`-1` 表示只发布一次。如果希望持续发布，可以使用 `-r` 指定频率：

```bash
rostopic pub -r 10 /cmd_vel geometry_msgs/Twist "{linear: {x: 0.2, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.0}}"
```

参数：

```text
-r 10
```

表示每秒发布 10 次。

持续发布适合正式遥控，因为可以不断刷新控制指令；停止发布后，当前 STM32 仍可能保持上一次目标速度，所以还需要发送全零速度或增加超时自动停车机制。

### 14.9 串口参数

当前 Python 串口对象的主要参数：

```python
serial.Serial(
    port='/dev/ttyUSB0',
    baudrate=115200,
    timeout=0.1
)
```

参数说明：

| 参数 | 当前值 | 含义 |
|---|---:|---|
| `port` | `/dev/ttyUSB0` | Nano 识别到的 USB 串口设备 |
| `baudrate` | `115200` | 每秒传输的符号速率，必须和 STM32 一致 |
| `timeout` | `0.1` | 串口读取等待超时时间，单位为秒 |
| 数据位 | 8 | 每个数据字符 8 位 |
| 校验位 | None | 不使用校验 |
| 停止位 | 1 | 使用 1 位停止位 |
| 流控制 | None | 不使用硬件流控 |

如果 Nano 下次识别成其他设备，例如 `/dev/ttyUSB1`，只需要修改 `port` 参数；波特率必须仍与 STM32 一致。

### 14.10 `stty` 串口配置命令

当前使用：

```bash
stty -F /dev/ttyUSB0 115200 cs8 -cstopb -parenb -ixon -ixoff -crtscts
```

参数说明：

| 参数 | 含义 |
|---|---|
| `-F /dev/ttyUSB0` | 指定要配置的串口设备 |
| `115200` | 设置波特率 |
| `cs8` | 8 位数据位 |
| `-cstopb` | 1 位停止位 |
| `-parenb` | 关闭校验位 |
| `-ixon` | 关闭软件流控接收 |
| `-ixoff` | 关闭软件流控发送 |
| `-crtscts` | 关闭硬件流控 |

### 14.11 当前消息结构的限制

当前 ROS 消息是连续速度格式：

```text
vx、vy、wz
```

但当前串口协议是单字符格式：

```text
W/S/A/D/Q/E/X
```

两者之间存在信息损失：

```text
ROS 的具体速度数值
  ↓
桥接节点只判断正负
  ↓
固定单字符指令
  ↓
STM32 固定速度 150
```

因此当前版本主要用于验证 ROS 通信链路，不是完整的连续速度底盘控制。

后续正式协议至少需要表达：

```text
四个轮子的目标速度
或 vx、vy、wz 三个目标速度
```

同时建议增加：

- 帧头
- 数据长度
- 有符号速度数据
- 校验和
- 序号或时间戳
- 接收确认
- 超时自动停车

