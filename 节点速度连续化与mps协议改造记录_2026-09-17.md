# 节点速度连续化与 m/s 协议改造记录

日期：2026-09-17  
项目：四轮电机速度监控与 ROS 底盘控制  
目标：将原来基于按键字符和“计数器/100 ms”的速度控制方式，改为连续发送、统一使用 m/s 的四轮速度控制方式。

## 1. 最终结论

本次目标已经完成并通过验证：

```text
/cmd_vel
  → ROS 计算四轮 m/s
  → 20 Hz 连续发送 V,m1,m2,m3,m4 帧
  → STM32 USART1 接收
  → strtof 解析
  → 四轮 m/s 目标值
  → PID 比较目标 m/s 和实际 m/s
  → PWM 驱动电机
```

已验证前进、后退、横移、原地旋转、停止、超时停止和实车低速闭环。

## 2. 改造前的问题

### 2.1 ROS 只发送方向字符

旧 ROS 节点读取 `/cmd_vel` 的 `vx`、`vy`、`wz`，但只比较三个量的大小，最后发送：

```text
W / S / A / D / Q / E / X
```

速度大小被丢弃，所以 ROS 的 `0.01 m/s` 和 `0.10 m/s` 可能都只变成同一个方向字符。

### 2.2 STM32 中的 150 不是 RPM

旧代码中的：

```c
Robot_Forward(150);
```

实际对应一个 100 ms 控制窗口内的编码器计数目标，含义接近：

```text
150 count / 100 ms
```

按当前 CPR 推算：

```text
11 PPR × 4 倍频 × 600 减速比 = 26400 count/output-shaft-revolution
```

因此：

```text
每个电机轴计数对应的输出轴计数约为 44 count/RPM
150 count/100 ms ≈ 3.41 RPM
```

轮子直径为 95 mm 时：

```text
轮径 D = 0.095 m
轮周长 C = π × 0.095 ≈ 0.2984513 m
150 count/100 ms ≈ 0.01696 m/s
```

所以原来的 `150` 应理解为约 `0.01696 m/s`，不是 `150 RPM`，也不是 `150 m/s`。

### 2.3 原来的计数方式是固定窗口

旧版每 100 ms 读取编码器增量后清零计数器，再用 count/100 ms 做 PID 输入。这种方式不便于和 ROS 的物理单位直接对应。

## 3. 使用的参数

### 3.1 电机、编码器和轮子

```text
电机铭牌速度：10 RPM，理解为减速箱输出轴空载速度
额定转速参考：约 7 RPM
编码器：11 PPR（当前按已知参数使用）
AB 计数：4 倍频
减速比：约 600:1
输出轴 CPR：26400 count/revolution
轮子直径：0.095 m
轮子周长：约 0.2984513 m
```

参考换算：

| 输出轴转速 | 100 ms 计数 | 轮缘速度 |
|---:|---:|---:|
| 1 RPM | 44 count | 0.00497 m/s |
| 3.41 RPM | 150 count | 0.01696 m/s |
| 7 RPM | 308 count | 0.03482 m/s |
| 10 RPM | 440 count | 0.04974 m/s |

### 3.2 车体几何

用户澄清：`10` 和 `12.25` 都是车体中心到轮子中心或轮胎宽度中心的距离，不是轮心距。

```text
Lx = 10 cm = 0.10 m
Ly = 12.25 cm = 0.1225 m
L  = Lx + Ly = 0.2225 m
```

## 4. 修改前的数据流向

```text
/cmd_vel Twist
  → ROS 回调读取 vx、vy、wz
  → 只判断主要运动方向，丢弃速度大小
  → 发送 W/S/A/D/Q/E/X 单字符
  → STM32 USART1 接收中断
  → uart_last_cmd + uart_cmd_ready
  → StartDefaultTask
  → Robot_Forward(150) 等固定值函数
  → Robot_SetVelocity
  → Motor_SetTarget
  → Motor_ControlStep 每 100 ms 运行
  → 读取计数增量并清零计数器
  → PID 比较 count/100 ms
  → PWM
```

这个链路的主要缺点是：方向有了，但速度大小没有传递；ROS 和 STM32 也没有统一的物理单位。

## 5. 修改后的数据流向

```text
/cmd_vel Twist
  → ROS 保存最新 vx、vy、wz
  → ROS 定时器每 50 ms 运行一次
  → 用车体几何计算 m1、m2、m3、m4，单位 m/s
  → 发送 V,m1,m2,m3,m4\n
  → STM32 USART1 接收中断逐字节缓存
  → 忽略 \r，遇到 \n 标记完整帧
  → StartDefaultTask 复制完整帧
  → ParseSpeedFrame + strtof 解析四个浮点数
  → Motor_SetTargetMps(1~4)
  → Motor_ControlStep 每 100 ms 运行
  → 读取连续运行的编码器计数器
  → current - previous 得到增量
  → 用实际 elapsed time 得到 count/s
  → count/s 换算为 m/s
  → PID 比较目标 m/s 和实际 m/s
  → PWM
```

当前 ROS 发送是连续的 20 Hz；当前 STM32 的底层 PID 调度周期仍是 100 ms，这是两个不同层级的周期。

## 6. STM32 端修改

涉及文件：

```text
-ros-/motor4_speed_monitor/Core/Inc/motor.h
-ros-/motor4_speed_monitor/Core/Src/motor.c
-ros-/motor4_speed_monitor/Core/Src/freertos.c
```

### 6.1 统一速度单位

新增或统一使用：

```c
#define MOTOR_ENCODER_CPR              26400.0f
#define MOTOR_WHEEL_DIAMETER_M         0.095f
#define MOTOR_WHEEL_CIRCUMFERENCE_M    (3.14159265359f * MOTOR_WHEEL_DIAMETER_M)
```

增加接口：

```c
void Motor_SetTargetMps(uint8_t motor, float target_mps);
float Motor_GetSpeedMps(uint8_t motor);
```

目标数组改为 m/s 语义：

```text
motor_target_mps[4]
```

原来的 `Motor_SetTarget()` 保留为兼容接口，但内部转发到 `Motor_SetTargetMps()`。

### 6.2 连续读取编码器并换算 m/s

新增调试量：

```c
volatile float debug_motor_speed_mps[4];
volatile float debug_motor_speed_cps[4];
volatile uint32_t debug_motor_dt_ms;
```

新增上一次计数器：

```c
static uint16_t motor_previous_counter[4];
```

`Motor_ControlStep()` 中不再每次读取后清零 TIM 计数器，而是：

```text
读取当前计数器
  → 当前值 - 上一次值
  → 保存当前值作为下一次的上一次值
  → 使用实际 elapsed_ms
  → 得到 count/s
  → 得到 m/s
```

换算公式：

```text
dt_s   = elapsed_ms / 1000
count/s = delta_count / dt_s
m/s    = count/s × wheel_circumference / encoder_cpr
```

差值使用 16 位有符号方式处理一次计数器回绕。

### 6.3 默认按键速度

原来的 `150 count/100 ms` 默认速度改成：

```c
#define DEFAULT_WHEEL_SPEED_MPS  0.0169575f
```

旧的按键命令仍保留兼容，但它们现在传入的是 m/s。

### 6.4 STM32 新协议

协议格式：

```text
V,m1,m2,m3,m4\n
```

例如：

```text
V,0.01696,0.01696,0.01696,0.01696
```

停止：

```text
V,0,0,0,0
```

STM32 接收端：

- 使用 64 字节帧缓存；
- 每次中断接收 1 个字节；
- `\r` 忽略；
- `\n` 表示帧结束；
- 帧完整后复制到局部数组；
- 使用 `ParseSpeedFrame()` 和 `strtof()` 解析；
- 解析成功后分别设置四个电机目标。

## 7. 为什么从 sscanf 改成 strtof

CubeIDE 编辑器在 `sscanf` 和参数区域显示红色划线，但工程能够编译成功。红线更可能是 CDT 索引器、标准库识别或中文路径造成的编辑器问题，不是编译器错误的直接证据。

不过，为了避免嵌入式环境中 `sscanf` 可能带来的栈开销和索引器识别问题，最终采用：

```text
自定义 ParseSpeedFrame()
  → strtof() 逐个读取四个浮点数
```

替换后：

- Python 端发送的浮点帧可以被正确解析；
- 四个 `debug_motor_target[]` 正确更新；
- STM32 编译成功并下载运行；
- 实车测试通过。

## 8. ROS 端修改

涉及文件：

```text
catkin_ws/src/fourdriver_base/scripts/fourdriver_serial_nano.py
```

### 8.1 发送方式

ROS 不再发送单字符方向命令，而是发送：

```python
frame = 'V,%.5f,%.5f,%.5f,%.5f\n' % (m1, m2, m3, m4)
```

### 8.2 连续发送

节点保存最新的：

```text
self.vx
self.vy
self.wz
```

通过 ROS 定时器每 50 ms 发送一次，即：

```text
20 Hz
```

因此速度不变时也会持续发送，不再依赖某一次按键或某一次 `/cmd_vel` 回调。

### 8.3 超时保护

默认参数：

```text
command_timeout = 0.5 s
```

如果超过 0.5 秒没有新的 `/cmd_vel`，节点将四个轮速设为 0，并持续发送零速帧。

节点启动和退出时也发送：

```text
V,0.00000,0.00000,0.00000,0.00000
```

## 9. 四轮运动公式

```python
rotation_term = (Lx + Ly) * wz

m1 = vx + vy - rotation_term
m2 = vx - vy + rotation_term
m3 = vx - vy - rotation_term
m4 = vx + vy + rotation_term
```

### 前进测试

```text
vx = 0.010 m/s
vy = 0
wz = 0
```

预期：

```text
m1 = m2 = m3 = m4 = 0.010 m/s
```

### 横移测试

```text
vx = 0
vy = 0.010 m/s
wz = 0
```

预期：

```text
m1 =  0.010 m/s
m2 = -0.010 m/s
m3 = -0.010 m/s
m4 =  0.010 m/s
```

### 原地旋转测试

```text
vx = 0
vy = 0
wz = 0.1 rad/s
```

因为 `L = 0.2225 m`，所以：

```text
L × wz = 0.2225 × 0.1 = 0.02225 m/s
```

预期：

```text
m1 = -0.02225 m/s
m2 =  0.02225 m/s
m3 = -0.02225 m/s
m4 =  0.02225 m/s
```

## 10. 遇到的问题和排查办法

### 10.1 ST-LINK 与 USB 串口

结论：ST-LINK 的 SWD 调试和 USB 串口的 USART1 通信可以同时使用。

当前分工：

```text
ST-LINK：下载、调试、Live Expressions
USB 串口：USART1 控制帧收发
```

但是同一个串口不能同时被串口助手和 ROS 节点打开。测试 ROS 时需要关闭串口助手，或者确保两者不是同一个串口设备。

### 10.2 串口乱码

串口助手曾显示乱码。打开十六进制显示后，发现包含：

```text
4D 6F 74 6F 72 20 73 70 65 65 64
```

可解码为：

```text
Motor speed
```

结论：115200、8 数据位、无校验、1 停止位正确；乱码来自启动中文信息的编码显示不一致，不是通信链路故障。

### 10.3 Source not found

CubeIDE 调试窗口显示找不到源码，路径中的中文目录出现了编码异常。

排查办法：

- 使用 `Locate File...` 重新定位源文件；
- 检查 Source Lookup Path；
- 执行工程索引重建；
- 直接观察 Live Expressions 是否正常刷新。

本次虽然源码定位异常，但程序编译、下载、运行和变量观察均正常。

### 10.4 串口没有回显

STM32 当前不会把收到的 `V,...` 帧回显到串口助手，所以串口窗口没有新增文字不代表失败。

正确检查方式：

```text
debug_rx_count
debug_motor_target[0..3]
```

测试中 `debug_rx_count` 从 `185` 增加到 `222`，说明串口字节已经进入 STM32。

### 10.5 TIM2->CNT 无法求值

直接输入：

```text
TIM2->CNT
```

可能无法被 Live Expressions 识别。可以改看：

```text
debug_motor_count[0]
debug_motor_speed_cps[0]
debug_motor_speed_mps[0]
debug_motor_dt_ms
debug_motor_target[0]
```

这些变量能直接观察完整的速度换算结果。

### 10.6 `sscanf` 红线但编译成功

红色划线是编辑器索引器提示，不能单独作为运行失败的证据。因为工程已经成功编译，所以编译器能够找到相关声明和实现。

最终通过换成 `strtof` 避开了这个问题，同时减少了对 `sscanf` 的依赖。

## 11. 实际验证记录

### 11.1 直接串口发送四轮速度

发送：

```text
V,0.01696,0.01696,0.01696,0.01696
```

结果：四个目标值均显示为约 `0.0169...`。

### 11.2 停止帧

发送：

```text
V,0,0,0,0
```

结果：四个目标值全部为 0，电机停止。

### 11.3 四路独立值

发送：

```text
V,0.01000,0.02000,0.03000,0.04000
```

结果：四个目标值按顺序分别更新，没有串位。

### 11.4 ROS 连续零速

ROS 节点启动后持续发送：

```text
V,0.00000,0.00000,0.00000,0.00000
```

说明 20 Hz 定时发送和启动零速正常。

### 11.5 ROS 前进

`vx=0.010 m/s` 时四轮目标值均为 `0.010 m/s`，测试通过。

### 11.6 ROS 原地旋转

`wz=0.1 rad/s` 时目标值为：

```text
[-0.02225, 0.02225, -0.02225, 0.02225] m/s
```

测试通过。

### 11.7 ROS 横移

`vy=0.010 m/s` 时目标值为：

```text
[0.010, -0.010, -0.010, 0.010] m/s
```

测试通过。

### 11.8 实车低速前进

使用 `vx=0.01696 m/s`，实际速度为正并逐渐接近目标，测试通过。

### 11.9 实车低速后退

使用 `vx=-0.010 m/s`，四个实际速度为负，停止发布后目标归零，测试通过。

## 12. 当前运行参数

### STM32

```text
USART1：115200，8N1
协议：V,m1,m2,m3,m4\n
编码器 CPR：26400
轮径：0.095 m
轮周长：约 0.2984513 m
底层电机控制周期：100 ms
```

### ROS

```text
默认端口：/dev/ttyUSB0
默认波特率：115200
连续发送频率：20 Hz
超时时间：0.5 s
Lx：0.10 m
Ly：0.1225 m
```

启动命令：

```bash
cd ~/catkin_ws
source devel/setup.bash
rosrun fourdriver_base fourdriver_serial_nano.py \\
  _port:=/dev/ttyUSB0 \\
  _baud:=115200
```

## 13. 尚未做的可选优化

1. STM32 的 `Motor_ControlStep()` 仍为 100 ms。如果要将底层 PID 也提高到 10 ms，需要同步重新检查 PID 的 `dt`、积分项、微分项和增益，不能只修改任务延时。
2. `MOTOR_ENCODER_CPR=26400` 是根据 11 PPR、四倍频和约 600 减速比得到的估算值。若要求更高的绝对速度精度，可使用已知转速实测 CPR。
3. 当前 ROS 节点没有新增 launch 文件，通常通过 `rosrun` 直接启动；后续可以增加 launch 文件统一配置端口、波特率和几何参数。
4. 建议后续创建 Git 提交，保存当前已验证版本，便于回退。

## 14. 一句话总结

系统已经从：

```text
按键字符 → 固定 150 count/100 ms → 电机
```

改成：

```text
/cmd_vel → 四轮 m/s → 连续 V 协议帧 → STM32 m/s PID → 电机
```

